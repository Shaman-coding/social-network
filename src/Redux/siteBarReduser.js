let defaultState = {
    friend:[
        {id:1, name: 'Kirill'},
        {id:2, name: 'Dasha'},
        {id:3, name: 'Maxim'}

    ]
}

const siteBarReduser = (state = defaultState, action) => {

    return state;
}

export default siteBarReduser;